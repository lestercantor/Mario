#pragma once

// Screen dimensions.
#define SCREEN_WIDTH 512
#define SCREEN_HEIGHT 416

// Amount of tiles on the map
#define MAP_WIDTH 16
#define MAP_HEIGHT 13

#define TILE_WIDTH 32
#define TILE_HEIGHT 32

#define SCREENSHAKE_DURATION 0.25f;

const float GRAVITY = 96.0f;
const float MovementSpeed = 0.01f;
const float JUMP_FORCE_DECREMENT = 400.0f;;
const float INITIAL_JUMP_FORCE = 400.0f;
const float KOOPA_SPEED = 96.0f;
const float INJURED_TIME = 4.5f;
const float ANIMATION_DELAY = 0.2f;